[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hpobench"
version = "0.0.1"
description = "Benchmark of hyperparameter optimization algorithms"
readme = "README.md"
authors = [
    {name = "Riccardo Doyle", email = "r.doyle.edu@gmail.com"}
]
requires-python = "<3.11"
license = {text = "Apache License 2.0"}
classifiers = [
    "Programming Language :: Python :: 3.10",
]
dependencies = [
    "pandas==1.3.5",
    "matplotlib==3.10.3",
    "scikit-learn==1.0.2",
    "optuna==4.4.0",
    "scikit-optimize==0.10.2",
    "numpy==1.26.4",
    "joblib==1.1.1",
    "cmaes==0.11.1",

    "yahpo_gym==1.0.2",
    "syne_tune[basic]==0.14.2",
    "huggingface-hub==0.33.4",
    "jahs-bench==1.1.0",
    "scikit-posthocs==0.11.4",
    "statsmodels==0.14.1",

    "ConfigSpace==0.4.21",
    "onnxruntime==1.16.3",
    "torch"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pre-commit",
    "autoflake",
    "pytest-xdist"
]

[project.urls]
Homepage = "https://github.com/rick12000/hpo-benchmark"

[tool.setuptools]
packages = { find = { where = ["."] , include = ["hpobench*"] } }
include-package-data = true
